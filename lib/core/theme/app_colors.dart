import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Primary brand green (buttons, logo, links, active states)
  static const Color primary = Color(0xFF2D7A4F);
  static const Color primaryDark = Color(0xFF1B5C38);
  static const Color primaryLight = Color(0xFF4CAF75);

  // Splash/onboarding background mint green
  static const Color splashBackground = Color(0xFFE8F5EE);
  static const Color splashShapeColor = Color(0xFFC8E6D4);

  // Onboarding illustration accent (yellow/amber)
  static const Color accent = Color(0xFFFFC107);
  static const Color accentDark = Color(0xFFFFB300);

  // Scaffold / page backgrounds
  static const Color background = Color(0xFFFFFFFF);
  static const Color surfaceBackground = Color(0xFFF9F9F9);

  // Text colors
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textHint = Color(0xFF9E9E9E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);
  static const Color textLink = Color(0xFF2D7A4F);

  // Input field
  static const Color inputBorder = Color(0xFFDDDDDD);
  static const Color inputFocusedBorder = Color(0xFF2D7A4F);
  static const Color inputFill = Color(0xFFFFFFFF);

  // Divider & shadow
  static const Color divider = Color(0xFFEEEEEE);
  static const Color shadow = Color(0x1A000000);

  // Semantic
  static const Color error = Color(0xFFD32F2F);
  static const Color success = Color(0xFF388E3C);
  static const Color warning = Color(0xFFFFA000);
}
